import { Meta } from "@storybook/addon-docs/blocks";
import { ApplicationsGrid, GovernanceNote } from "./_brand-specimens";

<Meta title="Brand/Applications" />

# Applications — تطبيق الهوية

Where the identity shows up across touchpoints. Each is a composition of the
pieces documented in this section — logo, brand palette, type, and patterns.

<ApplicationsGrid />

For website surfaces, build these from `@azeer/website-ui` sections (`Hero*`,
`FeatureGrid`, `Footer`, `Navbar`, …) plus the brand pieces (`AzeerLogo`,
`BrandPattern`) and brand utilities (`bg-brand-*`). See **Brand → Usage in Code**.

## Governance — هام

<GovernanceNote />
